% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mm.R
\name{fe_mm_by_group}
\alias{fe_mm_by_group}
\title{Estimate Marginal Means for all group levels (with bootstrap CIs)}
\usage{
fe_mm_by_group(
  data,
  group_var,
  feature_vars,
  outcome_var = "choice",
  respondent_id = "id",
  weight_var = NULL,
  offset = "yes",
  boot_reps = 999,
  conf_level = 0.95,
  variable_lookup = NULL,
  level_order = NULL,
  cluster_var = NULL,
  reverse = TRUE,
  header = TRUE,
  seed = 123,
  parallel = "no",
  ncpus = parallel::detectCores() - 1
)
}
\arguments{
\item{data}{Conjoint data frame.}

\item{group_var}{Name of the factor variable used to split the sample
(e.g. \code{"gender"}).}

\item{feature_vars}{Character vector of conjoint attribute columns.}

\item{outcome_var}{Name of the outcome column (default \code{"choice"}). Can
be both continuous and binary.}

\item{respondent_id}{Name of the unique observation identifier
(default \code{"id"}). Set to \code{NULL} to estimate without
respondent fixed effects (plain OLS with HC-robust SEs
and unstratified bootstrap).}

\item{weight_var}{Optional column name of post-stratification weights.
When supplied the model is weighted and the mean fixed
effect is computed as a weighted mean (default \code{NULL}).}

\item{offset}{Adjust for within-subgroup mean (default \code{"yes"}).}

\item{boot_reps}{Bootstrap replications (default \code{999}).}

\item{conf_level}{Confidence level for intervals (default \code{0.95}).}

\item{variable_lookup}{Optional data frame with two columns - \code{variable} and
\code{variable_nice} - for user-defined variable labels.
Use \code{NULL} to skip labelling.}

\item{level_order}{Optional character vector giving the desired within-attribute
order of the attribute levels. If \code{NULL} the data's factor-level order is
used.}

\item{cluster_var}{Optional column name used for clustering standard
errors and stratifying the bootstrap. When \code{NULL}
(default), clustering uses \code{respondent_id}. Set this
when you want to cluster by respondent but estimate
without individual fixed effects
(\code{respondent_id = NULL}).}

\item{reverse}{Reverse the \code{term_nice} factor order so the first
attribute plots at the top of a horizontal
\code{y = term_nice} plot (default \code{TRUE}). \code{term_nice} is
returned as an ordered factor - attributes in
\code{feature_vars} order, levels in their data factor order,
with one bold-faced header row per attribute - so it can
be plotted directly without any manual re-levelling.}

\item{header}{Insert one bold-faced header row per attribute
(default \code{TRUE}). Set \code{FALSE} to omit the header rows;
\code{term_nice} is still returned as the same ordered factor,
just without the header levels.}

\item{seed}{Random seed for reproducibility (default \code{123}).}

\item{parallel}{Parallelization for \code{boot::boot}: \code{"multicore"}
(Unix/Mac), \code{"snow"} (Windows), or \code{"no"}
(default \code{"no"}).}

\item{ncpus}{Number of CPUs when \code{parallel != "no"}
(default \code{parallel::detectCores() - 1}).}
}
\value{
A tidy data frame.
}
\description{
Iterates \code{\link[=fe_mm]{fe_mm()}} over every level of \code{group_var} and binds the results.
}
