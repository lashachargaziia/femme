% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{predict_preference}
\alias{predict_preference}
\title{Predict scenario-level choice probabilities.}
\usage{
predict_preference(
  data,
  scenarios,
  group_label,
  weight_var = "wt",
  outcome_var = "choice",
  respondent_id = "id",
  cluster_var = NULL,
  feature_vars,
  boot_reps = 999,
  order = 1,
  shrinkage = "none",
  conf_level = 0.95,
  lasso_seed = 123,
  seed = 123,
  parallel = "no",
  ncpus = parallel::detectCores() - 1
)
}
\arguments{
\item{data}{Conjoint data frame with. Requires a pre-computed weight column.}

\item{scenarios}{Named list of scenario vectors.}

\item{group_label}{String label added to every output row.}

\item{weight_var}{Name of the pre-computed weight column (default \code{"wt"}).}

\item{outcome_var}{Name of the outcome column (default \code{"choice"}). Can
be both continuous and binary.}

\item{respondent_id}{Name of the unique observation identifier
(default \code{"id"}). Set to \code{NULL} to estimate without
respondent fixed effects (plain weighted OLS with
unstratified bootstrap).}

\item{cluster_var}{Optional column name used for clustering standard
errors and stratifying the bootstrap. When \code{NULL}
(default), clustering uses \code{respondent_id}. Set this
when you want to cluster by respondent but estimate
without individual fixed effects (\code{respondent_id = NULL}).}

\item{feature_vars}{Character vector of conjoint attribute columns.}

\item{boot_reps}{Bootstrap replications (default \code{999}).}

\item{order}{Interaction order (default \code{1}). \code{1} fits main effects
only; \code{2} adds all pairwise interactions, and so on.}

\item{shrinkage}{\code{"LASSO"} or \code{"none"} (default \code{"none"}).}

\item{conf_level}{Confidence level (default \code{0.95}).}

\item{lasso_seed}{Seed for LASSO CV (default \code{123}).}

\item{seed}{Seed for bootstrap reproducibility (default \code{123}).}

\item{parallel}{Parallelization for \code{boot::boot}:
\code{"multicore"} (Unix/Mac), \code{"snow"} (Windows),
or \code{"no"} (default \code{"no"}).}

\item{ncpus}{Number of CPUs when \code{parallel != "no"}
(default \code{parallel::detectCores() - 1}).}
}
\value{
Data frame of scenario estimates and pairwise differences.
}
\description{
Predict scenario-level choice probabilities.
}
