% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise_inference.R
\name{fe_mm_pairwise_within}
\alias{fe_mm_pairwise_within}
\title{Within-subgroup pairwise MM differences with bootstrap inference}
\usage{
fe_mm_pairwise_within(
  data,
  group_var,
  feature_vars,
  outcome_var = "choice",
  respondent_id = "id",
  cluster_var = NULL,
  boot_reps = 999,
  conf_level = 0.95,
  offset = "yes",
  pretty = FALSE,
  pretty_scale = 1,
  pretty_suffix = "\%",
  pretty_digits = 1,
  seed = 123,
  parallel = "no",
  ncpus = parallel::detectCores() - 1
)
}
\arguments{
\item{data}{Conjoint data frame.}

\item{group_var}{Name of the factor variable used to split the sample
(e.g. \code{"gender"}).}

\item{feature_vars}{Character vector of conjoint attribute columns.}

\item{outcome_var}{Name of the outcome column (default \code{"choice"}). Can
be both continuous and binary.}

\item{respondent_id}{Name of the unique observation identifier
(default \code{"id"}). Set to \code{NULL} to estimate without
respondent fixed effects (plain OLS with HC-robust SEs
and unstratified bootstrap).}

\item{cluster_var}{Optional column name used for clustering standard
errors and stratifying the bootstrap. When \code{NULL}
(default), clustering uses \code{respondent_id}. Set this
when you want to cluster by respondent but estimate
without individual fixed effects (\code{respondent_id = NULL}).}

\item{boot_reps}{Bootstrap replications (default \code{999}).}

\item{conf_level}{Confidence level (default \code{0.95}).}

\item{offset}{Adjust for within-subgroup mean (default \code{"yes"}).}

\item{pretty}{Collapse each difference into a single readable string
column (e.g. \code{"15.2\% (CI [13.6; 16.8]), p-value = 0.000"}) instead of separate \code{estimate}/\verb{ci_*}/\code{p}
columns (default \code{FALSE}, which keeps the numeric
columns). In \code{\link[=fe_mm_pairwise_across]{fe_mm_pairwise_across()}} one such column
is produced per subgroup pair, named after the pair;
in \code{\link[=fe_mm_pairwise_within]{fe_mm_pairwise_within()}} the string lands in a
single \code{difference} column.}

\item{pretty_scale}{Multiplier applied to the estimate and CI bounds when
\code{pretty = TRUE} (default \code{1}; use \code{100} for a 0-1
outcome).}

\item{pretty_suffix}{String appended to each formatted number when
\code{pretty = TRUE} (default \code{"\%"}).}

\item{pretty_digits}{Decimal places for the estimate and CI when
\code{pretty = TRUE} (default \code{1}).}

\item{seed}{Random seed (default \code{123}) used to reproduce results.}

\item{parallel}{Parallelization for \code{boot::boot}: \code{"multicore"}
(Unix/Mac), \code{"snow"} (Windows), or \code{"no"}
(default \code{"no"}).}

\item{ncpus}{Number of CPUs when \code{parallel != "no"}
(default \code{parallel::detectCores() - 1}).}
}
\value{
A tidy data frame.
}
\description{
For each level of \code{group_var}, computes all pairwise differences between
attribute levels within that subgroup, together with two-sided p-values.
}
\examples{
\dontrun{
fe_mm_pairwise_within(
  data          = icecream,
  group_var     = "gender",
  feature_vars  = c("color", "taste", "cost"),
  boot_reps     = 100
)
}
}
