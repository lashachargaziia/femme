% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amce.R
\name{fe_amce_by_group}
\alias{fe_amce_by_group}
\title{Estimate AMCEs for all levels of a grouping variable}
\usage{
fe_amce_by_group(
  data,
  group_var,
  feature_vars,
  outcome_var = "choice",
  respondent_id = "id",
  cluster_var = NULL,
  variable_lookup = NULL,
  level_order = NULL,
  reverse = TRUE,
  header = TRUE
)
}
\arguments{
\item{data}{Conjoint data frame.}

\item{group_var}{Name of the factor variable used to split the sample
(e.g. \code{"gender"}).}

\item{feature_vars}{Character vector of conjoint attribute columns.}

\item{outcome_var}{Name of the outcome column (default \code{"choice"}). Can
be both continuous and binary.}

\item{respondent_id}{Name of the unique observation identifier
(default \code{"id"}). Set to \code{NULL} to estimate without
respondent fixed effects (plain OLS with HC-robust SEs).}

\item{cluster_var}{Optional column name used for clustering standard
errors. When \code{NULL} (default), clustering uses
\code{respondent_id}. Set this when you want to cluster by
respondent but estimate without individual fixed effects
(\code{respondent_id = NULL}).}

\item{variable_lookup}{Optional data frame with two columns - \code{variable} and
\code{variable_nice} - for user-defined variable labels.
Use \code{NULL} to skip labelling.}

\item{level_order}{Optional character vector giving the desired within-attribute
order of the attribute levels. If \code{NULL} the data's factor-level order is
used.}

\item{reverse}{Reverse the \code{term_nice} factor order so the first attribute
plots at the top of a horizontal \code{y = term_nice} plot (default \code{TRUE}).
\code{term_nice} is returned as an ordered factor - attributes in \code{feature_vars}
order, levels in their data factor order, with one bold-faced header row per
attribute - so it can be plotted directly without any manual re-levelling.}

\item{header}{Insert one bold-faced header row per attribute (default \code{TRUE}).
Set \code{FALSE} to omit the header rows; \code{term_nice} is still returned as the
same ordered factor, just without the header levels.}
}
\value{
A stacked data frame (one block per group level) with the same
columns as \code{\link[=fe_amce]{fe_amce()}}, plus header rows that bold-face the attribute
name for convenient plotting.
}
\description{
A wrapper around \code{\link[=fe_amce]{fe_amce()}} that iterates over every factor level of
\code{group_var} and binds the results.
}
\examples{
\dontrun{
fe_amce_by_group(
  data            = icecream,
  group_var       = "gender",
  feature_vars    = c("color", "taste", "cost"),
  variable_lookup = my_lookup
)
}
}
